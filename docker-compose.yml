version: '3'

services:
    db:
        image: postgres
        environment:
            POSTGRES_DB: airline
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres

    web:
        build: .
        volumes:
            - .:/Desktop/Lecture1/Container
        ports:
            - "8000:8000"